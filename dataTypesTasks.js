//Perform addition of various types (string + boolean, string + number, number + boolean)
let string = "JavaScript";
let boolean = true;
let number = 8;

console.log(string + boolean); //string + boolean
console.log(string + number); // string + number
console.log(number + boolean); // number + boolean

//Perform multiplication of various types (string * boolean, string * number, number * boolean)

console.log(string * boolean); //string * boolean
console.log(string * number); // string * number
console.log(number * boolean); // number * boolean

//Divide different types (string / boolean, string / number, number / Boolean)

console.log(string / boolean); //string / boolean
console.log(string / number); // string / number
console.log(number / boolean); // number / Boolean

//Perform explicit conversion (number, string, boolean)

let num2 = 35;//type of is 'number'
let str2 = String (num2);//explicitly convert a number to a string

let str = '555'; //type of is 'string'
let num = Number (str);//explicitly convert a string to a number

let bool = true; //type of is 'boolean'
let num3 = Number (bool); //explicitly convert boolean to a number

